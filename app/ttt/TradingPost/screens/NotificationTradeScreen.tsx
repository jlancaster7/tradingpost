import {View} from "react-native";

export const NotificationTradeScreen = (props: any) => {
    return (
        <View style={{flex: 1, backgroundColor: "#F7f8f8"}}>
            Trade Notifications Screen
        </View>
    )
}