import Extension from "./index";
import {NotificationSubscriptionTypes} from "../../../notifications/interfaces";

export default class extends Extension {
    //toggle = this._makeFetch<{ type: NotificationSubscriptionTypes, typeId: number, disabled: boolean, data?: {} }>("toggle", this._defaultPostRequest)
    //getStatus = this._makeFetch<{ type: NotificationSubscriptionTypes, typeId: number }, { disabled: boolean }>("getStatus", this._defaultPostRequest)
}