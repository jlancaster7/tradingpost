// import { Entity } from "../src/shared/interfaces/Entity";
// import { execProc } from "./api/database";
// import { ListObjectsCommand, PutObjectCommand, S3Client } from '@aws-sdk/client-s3'

// // execProc<Entity>("listEntity").then((entities) => {
// //     entities.forEach((e) => {


// // client.send(new PutObjectCommand({
// //             Bucket: "entity-manager",
// //             // Add the required 'Key' parameter using the 'path' module.
// //             Key: e.name.replace(/(?:^|\.?)([A-Z])/g, function (x, y) { return "_" + y.toLowerCase() }).replace(/^_/, "") + ".json",
// //             // Add the required 'Body' parameter
// //             Body: e.definition ? JSON.stringify(e.definition) : "",
// //         }))
// //     })
// // })

